#include <turnkey/base>
#include <turnkey/samba>
#include <turnkey/ajaxplorer>
#include <turnkey/compression>

mldonkey-server
netcat

clamav-daemon

stunnel             /* ssl support for mldonkey and webgmui */

libxml2-utils       /* used for broadcatching (xmllint) */
xsltproc            /* used for broadcatching */

postfix             /* mldonkey email support */
webmin-postfix

ftp
wget

